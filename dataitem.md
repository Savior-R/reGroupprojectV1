## 难点一：虚拟学生实时互动与课堂行为模拟

- **难点描述**：虚拟学生要能 "像真人一样" 举手、提问、回答（对 / 错 / 不完整）、走神、小声说话，并针对试讲者的提问做出合理应答。行为不能是机械定时触发，需结合试讲者话语内容与教学环节动态发生，拟真度与响应实时性难以两全。
- **涉及模块**：AI 虚拟课堂、试讲会话
- **解决方案**：
  1. **行为配置驱动**：`virtual_classroom.behavior_config` 预置专注 / 走神 / 举手 / 提问 / 说话概率，用状态机 + 随机事件发生器沿时间轴触发；
  2. **语义理解**：实时转写试讲者语音 → 意图识别（提问 / 点名 / 讲解 / 维持纪律）→ 匹配虚拟学生应答库；
  3. **分层响应**：高频规则应答保低延迟，低频 LLM 深度应答（追问、质疑）走异步队列，不阻塞试讲主线。
- **实现要点**：三种模式（自由试讲 / 情景模拟 / 结构化面试）复用同一套互动引擎，事件全部落库 `classroom_interaction` 可回放。

## 难点二：多模态 AI 教学能力自动评测

- **难点描述**：评测覆盖教学内容、表达、教姿教态、互动控场、板书 / PPT、时间管理 6 大维度，数据源涉及语音、视频、板书图像、PPT 四类异构数据，需在同一时间轴上对齐并融合打分；任一单模型误差都会传导到总分。
- **涉及模块**：AI 多维度评测
- **解决方案**：
  1. **分模块流水线**：ASR + 发音评分（普通话 / 语速 / 停顿 / 音量 / 口头禅）→ 视觉姿态模型（站姿 / 手势 / 眼神 / 走动）→ 板书 OCR → 教学环节切分（导入 / 新授 / 练习 / 小结 / 作业）；
  2. **统一时间轴**：所有指标携带 start/end 时间戳写入 `evaluation_detail`，逐项附 evidence 音视频片段；
  3. **可配置权重**：评分标准 JSON 驱动维度权重，教师自定义细则可覆盖默认权重。
- **实现要点**：评测明细可逐项回溯 "哪句话、哪个动作、哪段板书扣分"，避免黑盒打分。

## 难点三：音视频多路同步录制与低延迟直播

- **难点描述**：一次试讲需同时采集人像、屏幕、板书、声音四路媒体并保持同步；录制文件体积大，云端存储与回看加载压力大；教师在线观摩要求低延迟且不能干扰试讲者。
- **涉及模块**：试讲录制与直播
- **解决方案**：
  1. **统一时钟 + 分段切片**：多路流写入统一会话时间戳，按片段（如 5 分钟）切片存储，解决大文件与断点续传问题；
  2. **存储分级**：本地热存 + 云端对象存储归档，回看按需拉流；
  3. **低延迟直播**：教师端 WebRTC / 低延迟 HLS 观看，观摩点评经 `observation_record` 异步写入。
- **实现要点**：录制 / 直播 / 回看三态共用同一 `session_id`，评测证据片段可直接定位到对应媒体。

## 难点四：逐句点评与问题时间轴定位

- **难点描述**：评测报告要指出 "哪里讲太快、哪里不清晰、哪里互动不足"，需把 AI 分析结果精确定位到具体句子、时间点和教学环节，并给出可执行的优化话术。
- **涉及模块**：即时反馈与改进建议
- **解决方案**：
  1. **词级时间戳**：ASR 输出带词级时间戳的转写入库 `trial_transcript`，VAD 切句；
  2. **证据切片**：将问题句前后 5~10 秒音视频切成 demo 片段，关联到 `sentence_feedback`；
  3. **LLM 生成建议**：以转写 + 评测指标为上下文生成逐句分析与优化话术（`improved_text` / `optimized_script`），审核后可沉淀为话术库。
- **实现要点**：报告从 "一串分数" 升级为 "可执行的教学改进清单"。

## 难点五：突发情景动态模拟与课堂纪律处理

- **难点描述**：高阶训练要求随机注入 "学生质疑、课堂混乱、设备故障" 等突发事件，触发时机不可预知才能训练临场应变；但事件要合理、不打断试讲主线，且难度可分级。
- **涉及模块**：训练模式（阶梯提升）
- **解决方案**：
  1. **剧本 + 随机触发器**：`scenario_config` 定义事件类型 / 触发条件 / 难度，事件生成器按教学环节和随机种子注入；
  2. **事件响应判定**：依据试讲者的处理（应答 / 引导 / 维持纪律话术）记录 `teacher_response` 并单独计分；
  3. **可打断可恢复**：事件期间录制不断流，试讲者可选择应对或叫停，评分按事件处理质量核算。
- **实现要点**：同一节课可生成多种事件组合，避免学生背题式训练。

## 难点六：AI 评分与教师批阅融合及达标认证判定

- **难点描述**：AI 自动评分与教师人工批阅标准需一致可信；证书发放、薄弱项推送依赖稳定可解释的判定规则；跨学期还需对比历史进步曲线。
- **涉及模块**：教师 / 管理员后台、达标与认证
- **解决方案**：
  1. **双轨评分**：AI 初评 + 教师复核（`grading_record`），不一致时以教师为准并留痕，反向校准 AI 权重；
  2. **认证规则引擎**：`certification_standard` 用 JSON 配置各证书的维度阈值，达标判定幂等可重算；
  3. **数据沉淀**：每次评测写入 `progress_record`，按学生聚合生成进步曲线与薄弱项推荐（`weak_point_training`）。
- **实现要点**：证书可追溯、标准可调整，AI 评分随人工复核数据积累越用越准